using Microsoft.EntityFrameworkCore;

namespace ASPNETCORE_MinimalAPI
{
    class TodoDb : DbContext
    {
        public TodoDb(DbContextOptions<TodoDb> options)
            : base(options) { }

        public DbSet<Todo> Todos => Set<Todo>();
    }
}
